{
  "scripts": {
    "build": "$(yarn bin)/tsc",
    "pretest": "npm run build",
    "test": "$(yarn bin)/mocha --compilers js:babel-core/register"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.34",
    "@types/node": "^6.0.53",
    "babel-core": "^6.21.0",
    "babel-preset-latest": "^6.16.0",
    "expect.js": "^0.3.1",
    "mocha": "^3.2.0",
    "raf": "^3.3.0",
    "typescript": "^2.1.4",
    "webpack": "2.2.0-rc.3"
  }
}
